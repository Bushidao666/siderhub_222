# Middleware (Express) — Escopo do subsistema de agents

Não aplicável neste subsistema. A orquestração em `.agents` não executa servidor HTTP.

Para middlewares do backend do produto, ver documentação geral em `../../architecture.md`.
