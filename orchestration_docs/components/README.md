# Components (React) — Escopo do subsistema de agents

Não aplicável neste subsistema. A orquestração em `.agents` é headless.

Para componentes do produto, ver documentação geral em `../../architecture.md`.
