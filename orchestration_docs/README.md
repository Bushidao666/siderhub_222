# Documentação — Sistema de Orquestração (.agents)

Esta pasta concentra toda a documentação do subsistema de orquestração baseado em arquivos JSONL e scripts em `.agents/bin`.

- Fluxo e visão geral: ver seção "Orquestração de Agents" em `../../architecture.md`.
- Contratos (JSON/JSONL/estado): `./interfaces/`
- Serviços/daemons: `./services/`
- Utilidades operacionais: `./utilities/`
- Stubs de escopo (não aplicáveis): `./api/`, `./hooks/`, `./components/`, `./middleware/`, `./schemas/`

Organização preserva links relativos entre interfaces ⇄ serviços ⇄ utilidades.
