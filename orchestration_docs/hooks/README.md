# Hooks (React) — Escopo do subsistema de agents

Não aplicável neste subsistema. A orquestração em `.agents` não define hooks React.

Para UI do produto, ver documentação geral em `../../architecture.md` e subpastas `components/` e `hooks/` (quando disponíveis).
