# Schemas (Database) — Escopo do subsistema de agents

O subsistema `.agents` não define schemas de banco; opera com arquivos JSONL e JSON locais.

Para schemas do produto (Prisma/PostgreSQL), ver `prisma/` e documentação em `../../architecture.md`.
