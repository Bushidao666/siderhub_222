# Gaps — Execution Status

Timestamp: 2025-11-03T11:30:01Z

- **Lesson detail still mocked** — `src/frontend/pages/Academy/LessonDetail.tsx:15` cria um lesson placeholder em memória e ignora `useCourseTree`/`useLessonComments`, impedindo jornada real com conteúdo, progresso e threads conforme PRD (seções 3.2 e 3.3). Sem dados reais não há condição de validar tracking, avaliações ou moderação.
- **Course detail sem árvore oficial** — `src/frontend/pages/Academy/CourseDetail.tsx:18` consome apenas `useHubData`, reutilizando cards da home. `useCourseTree` nunca é chamado, logo módulos/lições e gating por drip não aparecem na UI.
- **Wizard do Hidra é apenas casca** — `src/frontend/pages/Hidra/Wizard.tsx:8` alterna texto estático com botões e não integra `hidraService` nem segmentos/templates vindos do backend, contrariando o fluxo multi-etapas descrito no PRD (§3.4).
- **Admin members depende de placeholder** — `src/frontend/hooks/useAdminMembers.ts:19` injeta dados estáticos porque o backend não expõe `/admin/members` (`src/backend/api/admin/index.ts` não possui rota equivalente) nem `AdminService` fornece listagem. Admins continuam sem visibilidade real sobre permissões/grupos.
- **Trabalhos assíncronos inexistentes** — `src/backend/jobs/` permanece vazio apesar das decisões de usar BullMQ para campanhas Hidra, métricas e limpeza. Sem workers o disparo em lote, retries e sync periódico continuam fora do ar.
- **Threads de comentários ausentes** — `src/backend/services/academy/AcademyService.ts:482` só trata comentários de primeiro nível; não há APIs/repos para replies e a UI rende `comments={[]}`. O PRD exige threads (até 3 níveis) e moderação com fila dedicada.
