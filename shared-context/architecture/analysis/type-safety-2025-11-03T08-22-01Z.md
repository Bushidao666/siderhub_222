# Type Safety — Shared Contracts & Usage

Timestamp: 2025-11-03T08:22:01Z

## Strengths
- Shared DTOs and enums used across layers: src/shared/types/*
- Zod schemas validate API payloads consistently (academy, admin, cybervault, hidra): src/backend/api/*/index.ts:1

## Findings
- `as any` placeholders for service wiring: src/backend/server.ts:64
- EvolutionClient generic `request<T = any>` weakens type flow: src/backend/services/hidra/EvolutionClient.ts:81
- TokenService casts `expiresIn` to any unnecessarily: src/backend/services/auth/TokenService.ts:37

## Recommendations
- Replace `any` service stubs with concrete DI of Prisma-backed services; remove casts.
- Narrow EvolutionClient `request` generics per call sites or return typed DTOs explicitly.
- Use `SignOptions['expiresIn']` type for TokenService to avoid `as any`.

