# Type Safety Audit

Timestamp: 2025-11-03T11:30:01Z

- ✅ `src/frontend/App.tsx` agora reutiliza `getQueryClient()` do singleton (`src/frontend/lib/queryClient.ts:30`), eliminando o duplo cache citado na auditoria anterior.
- ⚠️ `src/frontend/lib/queryClient.ts:69` define `queryKeys.hidra.campaignStats()` com a mesma key de `hidra.dashboard()`, sobrepondo caches distintos (overview vs. timeline). Collisions quebram invalidations seletivas e podem mascarar dados obsoletos.
- ⚠️ `src/backend/services/hidra/EvolutionClient.ts:128` mantém `request<T = any>`, permitindo que chamadas sem schema retornem `any`. Obrigatoriedade de generic explícito ou wrappers especializados evita perdas de tipagem no consumo de Evolution API.
