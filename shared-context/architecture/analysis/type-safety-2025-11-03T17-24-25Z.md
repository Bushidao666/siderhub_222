# Type Safety Audit — Queues Refresh

Timestamp: 2025-11-03T17:24:25Z

- ⚠️ `src/backend/jobs/queues/index.ts:25` injeta `connection: { url: cfg.url } as any` e retorna `QueueBundle` sem validar `ConnectionOptions`. Acoplado a isso, `src/backend/jobs/connection.ts:23` converte `ConnectionOptions` com `as unknown as any`, anulando checagens em tempo de compilação para BullMQ.
- ⚠️ Workers (`CampaignDispatchWorker.ts:23`, `MetricsSyncWorker.ts:23`, `CleanupWorker.ts:23`) repetem `as any` na configuração da conexão. Além de violar o contrato do BullMQ, esconde erros de configuração (ex.: parâmetros ausentes) e inviabiliza o uso de `QueueScheduler` compartilhado.
- ✅ Restante da tipagem dos serviços Academics/Hidra permanece coerente após adição de replies/moderação.
